# 開発ループ

このプロジェクトは、1つの小さなIssueを完成させてから次へ進む。

## 1サイクル

1. GitHub Issuesから優先度が最も高い未完了Issueを1件選ぶ
2. Issueの目的、完了条件、対象外を確認する
3. プレイヤーが実際に体験できる最小変更だけを実装する
4. 自動チェックとスマホ実機で確認する
5. 5分間プレイして結果をIssueに記録する
6. 問題がなければ小さいコミットを作成してpushする
7. Issueを閉じ、親Issueのチェックリストを更新する
8. 次のIssueへ進む

## AIが1回に守る範囲

- 同時に進めるIssueは1件だけ
- 受け入れ条件にない機能は追加しない
- 既存のゲームループを壊さない
- 変更後は`node --check game.js`と`node --check sw.js`を実行する
- UIを変更した場合はスマホ幅で確認する
- 完了時に変更内容、検証結果、残った問題をIssueへ記録する

## 完了の定義

- Issueの受け入れ条件をすべて満たしている
- 既存機能が動作する
- iPhoneまたはAndroidの実機で操作できる
- GitHub Actionsのチェックが成功している
- 変更が小さいコミットにまとまっている
- `TASKS.md`と親Issueの状態が更新されている

## 失敗した場合

Issueは閉じず、再現手順とエラーを記録する。修正が大きくなる場合は、別の小Issueに分解してから続ける。
